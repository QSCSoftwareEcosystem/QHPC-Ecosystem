Bootstrap: docker
From: ubuntu:24.04

%labels
    org.qscsoftware.environment "python-lib"
    org.qscsoftware.project "QHPC-Ecosystem"

%post
    export DEBIAN_FRONTEND=noninteractive
    apt-get update
    apt-get install -y --no-install-recommends \
        build-essential \
        ca-certificates \
        git \
        python3 \
        python3-dev \
        python3-pip \
        python3-venv
    rm -rf /var/lib/apt/lists/*

%environment
    export LANG=C.UTF-8
    export LC_ALL=C.UTF-8
    export QHPC_ENVIRONMENT_CLASS=python-lib
    export PYTHONUNBUFFERED=1

%runscript
    exec "$@"

%test
    command -v git
    command -v python3
