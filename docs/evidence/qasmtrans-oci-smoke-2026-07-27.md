# QASMTrans OCI Build and Smoke Evidence

- Date: 2026-07-27
- Scope: local OCI build and smoke verification
- Acceptance state: `oci-smoke-tested`
- Target acceptance: not performed

## Inputs

| Item | Pinned value |
| --- | --- |
| Source | `https://github.com/pnnl/qasmtrans` |
| Revision | `1843c98fa4bac9cf6b88412145b69457e9176124` |
| Git archive | `sha256:57a76bda6d6d440f54a22981972ccf24b647a5a13ffdec7c28d3efedfd4c5a3f` |
| Source date epoch | `1764775929` |
| Runtime contract | `containers/operations/qasmtrans/runtime.yaml` |
| Recipe | `sha256:ae6b180750248afb7e224393880b9ff52a72f8e12d6622d7c206837e670dbd3c` |
| Builder base | `gcc:13.4.0-bookworm@sha256:69004890c1c5fa2deb855aecc4033afa507c5d187b89e2bce4eb1d907101e867` |
| Runtime base | `debian:bookworm-slim@sha256:63a496b5d3b99214b39f5ed70eb71a61e590a77979c79cbee4faf991f8c0783e` |

The build context was generated by `qhpc-ecosystem operation-runtime prepare`.
The tool reproduced the declared Git archive digest, verified the recipe,
wrapper, and fixture digests, assigned the source commit timestamp to context
files, and generated deterministic build metadata. The build used no package
manager and disabled network access for build steps.

## Local OCI Result

- Docker Engine: `29.6.2`
- Builder host: Linux `aarch64`
- Requested image platform: `linux/amd64`
- Local tag: `qhpc/qasmtrans:1843c98-linux-amd64`
- Local image ID: `sha256:28e761efbd030c01e51edb96c7da6bafed7f18155c5b3973aa54aa030dbac9df`
- Image size: `28,747,432` bytes
- Runtime user: `65532:65532`
- Entrypoint: `/opt/qhpc/bin/qhpc-qasmtrans`

Two no-cache builds emitted the same platform payload manifest,
`sha256:28e761efbd030c01e51edb96c7da6bafed7f18155c5b3973aa54aa030dbac9df`.
BuildKit's automatically generated provenance manifests were not stable and
therefore produced different outer indexes. The QHPC Docker build command now
uses `--provenance=false`; a policy-approved signed attestation remains a
separate release gate.

## Smoke Verification

The contracted smoke runner used:

- no network;
- a read-only root filesystem;
- all Linux capabilities dropped;
- `no-new-privileges`;
- a read-only `/inputs` bind;
- a writable `/outputs` bind;
- the fixed IBM Toronto topology and `ibmq` mode; and
- no caller-provided command arguments.

The final run completed in `335 ms`. It created a `174` byte OpenQASM output at
`/outputs/circuit.qasm`, contained the required `OPENQASM 2.0;` marker, and had
the run-specific checksum
`sha256:b34f9486b780b324f6ca42686c569f728b5b1e51d576a1677a9a009169128652`.
An additional run with a caller-provided `--help` argument was rejected by the
wrapper with exit status `64`.
QASMTrans initializes routing from `std::random_device`, so output checksums may
vary between valid runs; provenance must retain the checksum from each run.

## Remaining Gates

This local image ID is not a publishable internal-registry digest and is not an
HPC acceptance result. The following remain pending:

- publish the OCI image to an approved internal registry by immutable digest;
- generate the required SBOM, vulnerability result, signature, and attestation;
- convert the immutable OCI release to SIF on an approved Linux build host;
- verify the SIF digest and signature before execution;
- execute through Slurm with an approved storage and bind profile;
- compare native and container correctness and performance on the target; and
- measure image staging, input staging, scheduler, execution, and output
  collection latency on the target.
